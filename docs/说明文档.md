# Camera 摄影站 说明文档

## 项目简介

面向摄影新手的教程站，同时作为个人作品集。极简黑白风格，支持深浅色切换。
需求详见 [需求文档.md](./需求文档.md)。

- 线上地址：https://worldgu.github.io/Camera/
- 部署方式：GitHub Actions 自动构建，推送 `main` 分支即发布

## 技术栈

| 项目 | 选型 |
|------|------|
| 框架 | Astro 7（静态输出） |
| 交互组件 | React 19（仅作品画廊使用） |
| 样式 | 原生 CSS（`src/styles/global.css` + 组件内联样式） |
| 内容 | Markdown 内容集合（教程）+ JSON（作品、装备） |
| 部署 | GitHub Pages，子路径 `base: '/Camera'` |

## 目录结构

```
Camera/
├── astro.config.mjs          # site / base / React 集成
├── public/
│   └── favicon.svg           # 光圈图形，随系统深浅色反色
├── src/
│   ├── content.config.ts     # learn 内容集合 schema
│   ├── content/learn/basics/ # 教程 Markdown，9 篇基础篇
│   ├── data/
│   │   ├── works.json        # 作品数据，12 条
│   │   └── gear.json         # 装备数据，17 件
│   ├── layouts/BaseLayout.astro
│   ├── components/
│   │   ├── Header.astro      # 汉堡菜单导航
│   │   ├── ThemeToggle.astro # 深浅色切换
│   │   ├── WorksGallery.tsx  # React：筛选 / 瀑布流 / 相册 / 灯箱
│   │   └── GearCard.astro
│   ├── pages/                # 路由，见下表
│   ├── styles/global.css     # 设计变量与全局样式
│   └── utils/                # slug.ts、gear.ts
├── docs/                     # 需求文档、说明文档、ai-memory
└── .github/workflows/deploy.yml
```

## 路由一览

| 路径 | 文件 | 说明 |
|------|------|------|
| `/` | `pages/index.astro` | 首页，全屏视觉 + 入口 |
| `/learn` | `pages/learn/index.astro` | 教程总览 |
| `/learn/path` | `pages/learn/path.astro` | 学习路径 |
| `/learn/topics` | `pages/learn/topics.astro` | 分类索引 |
| `/learn/{category}/{slug}` | `pages/learn/[category]/[slug].astro` | 文章详情，上下篇 + 分享 |
| `/works` | `pages/works/index.astro` | 作品画廊 |
| `/works/{slug}` | `pages/works/[slug].astro` | 作品详情 |
| `/gear` | `pages/gear/index.astro` | 装备：在用 / 曾用 / 愿望 |
| `/gear/{slug}` | `pages/gear/[slug].astro` | 器材详情 + 关联作品 |
| `/lab` | `pages/lab/index.astro` | 实验室入口，两个实验的卡片 |
| `/lab/camera` | `pages/lab/camera/index.astro` | 相机模型，Three.js 程序化 A7C2，7 视角 + 14 个可交互部件，含自动旋转与部件热点标记 |
| `/lab/simulator` | `pages/lab/simulator/index.astro` | 曝光模拟器，支持 URL query 预设 |
| `/about` | `pages/about/index.astro` | 关于 |

当前构建产物共 79 个页面。

## 出图能力

项目内置了火山方舟 Seedream 的调用脚本，用于生成首页 hero、作品占位图、
教程示意图等网站用图。工具路径：`scripts/ark_image.py`，只用 Python 标准库，
零依赖。

### 配置

```bash
export ARK_API_KEY="ark-xxxxxxxx"
```

API Key 优先级：`--key` 参数 > 环境变量 `ARK_API_KEY`。不要把 Key 提交到仓库。

### 常用命令

```bash
# 生成首页 hero（横版 16:9，2560x1440）
python3 scripts/ark_image.py -p "提示词" --size 16:9 -o public/images/hero.jpg

# 生成方图
python3 scripts/ark_image.py -p "提示词" --size 1:1 -o output/square.jpg

# 图生图（参考本地文件）
python3 scripts/ark_image.py -p "换成夜景" --image ref.jpg --size 4:3 -o out.jpg

# 生成多张变体
python3 scripts/ark_image.py -p "提示词" -n 4 -o output/var.jpg
```

### 关键约束

- 模型：`doubao-seedream-5.0-lite`
- 像素下限：**总像素 >= 3,686,400**，低于此数会报 `InvalidParameter`
- 推荐尺寸映射：
  - 16:9 → 2560 × 1440
  - 9:16 → 1440 × 2560
  - 1:1 → 2048 × 2048
  - 4:3 → 2496 × 1664
  - 3:4 → 1664 × 2496
- 返回的 TOS 链接 24 小时过期，脚本会自动下载到本地

### 其他路线对照

| 路线 | 状态 | 备注 |
|------|------|------|
| 火山方舟 Seedream | 可用 | 本项目主路线，脚本已就位 |
| Codex 内置 image_gen | 不可用 | 当前环境未加载 |
| OpenAI CLI (gpt-image-2) | 不可用 | 需要 OPENAI_API_KEY + openai 包 |

## 开发环境

要求 **Node >= 22.12.0**（与 CI 一致，CI 用 `node-version: "22"`）。

这里有个容易踩的坑：`astro@7.2.1` 硬性要求 `>=22.12.0`，Node 22.11 会被直接拒绝启动。
更麻烦的是，用 22.11 跑 `npm ci` 时，`@rolldown/binding-win32-x64-msvc` 这个 native
binding 自身的 `engines` 也写着 `>=22.12.0`，作为可选依赖会被 npm **静默跳过**；
之后即使换成合规的 Node，也会报 `Cannot find native binding`。遇到这个报错，
换 22.12+ 重新 `npm ci` 即可，不必按提示删 lockfile。

另外，如果 `npm ci` 报 `EPERM: operation not permitted, unlink ...rolldown-binding...node`，
通常是有残留的 `astro preview` / `astro dev` 进程锁着该文件，先结束 node 进程再装。

```bash
npm install        # 安装依赖
npm run dev        # 本地开发，默认 http://localhost:4321/Camera/
npm run build      # 构建到 dist/
npm run preview    # 预览构建产物
```

注意 `base` 是 `/Camera`，本地访问也必须带这个前缀，直接开 `localhost:4321` 会 404。

## 访问统计配置

统计走 Google Analytics，通过环境变量 `PUBLIC_GA_ID` 控制，只在生产构建时注入脚本：

- 本地：把 `.env.example` 复制为 `.env`，填入 `G-XXXXXXXXXX`
- 线上：在 GitHub 仓库 Settings → Secrets and variables → Actions → Variables 添加 `PUBLIC_GA_ID`

未配置时构建产物里不含任何统计脚本，可用 `rg googletagmanager dist` 验证。

## 内容维护

新增教程：在 `src/content/learn/basics/` 下加 Markdown，frontmatter 字段见
`src/content.config.ts`（`title`、`description`、`category`、`order`、`date`、`tags`）。
frontmatter 里没有 `slug` 字段，URL 由文件名去掉数字前缀和 `.md` 生成，统一走
`src/utils/slug.ts` 的 `slugOf()`。学习路径页、分类索引页和详情页的 `getStaticPaths`
必须都用这个函数，否则链接会和实际路由对不上。

新增教程图解：正文里直接写 `<figure class="diagram">` 包一个内联 `<svg>`，末尾配
`<figcaption>` 说明。SVG 里不要写死颜色，只用 `global.css` 里定义好的 class：
`dia-stroke`（主描边）、`dia-stroke-thin`（辅助线）、`dia-fill`（实心主体）、
`dia-fill-soft`（浅底色块）、`dia-text` / `dia-text-muted` / `dia-text-label`（三级文字）。
`dia-text-invert`（`fill: var(--bg)`，深色块上的反白文字）。
这些 class 都绑在 CSS 变量上，深浅色主题才会跟着一起变。`viewBox` 请用相对坐标并配
`role="img"` + `aria-label`；宽度交给 CSS 的 `max-width: 100%`，不要写死 `width`，
否则手机端会溢出。示例见 `02-aperture.md`、`03-shutter-speed.md`、`08-focal-length.md`。

新增作品：往 `src/data/works.json` 追加一条，`slug` 唯一。目前没有真实图片，靠
`color` + `aspectRatio` 生成 CSS 渐变占位图。

新增器材：往 `src/data/gear.json` 追加一条。`status` 取 `active` / `retired` /
`wishlist`，`type` 取 `camera` / `lens` / `support` / `light` / `filter` /
`storage` / `bag`。`matchCamera`、`matchLens` 的值要和 `works.json` 里的
`camera`、`lens` 字符串完全一致，装备详情页靠它反查关联作品。

## 已知缺口

- 全站没有真实照片，作品与器材图都是 CSS 渐变占位
- EXIF 自动提取未实现（CI 无 exiftool 步骤），作品参数目前手写在 JSON 里
- i18n 未预留，文案硬编码在各页面模板中
- 曝光模拟器的场景用程序化 Canvas 绘制，不是需求原定的真实照片分层素材

（教程图解已在 v1.5 补齐，七大分类 39 篇共 44 张，此处不再列为缺口）

## 维护约定

- lockfile 请用 Node 22 配套的 npm 10 生成。npm 11 会按当前平台裁剪 lockfile，
  删掉 Linux 才需要的 `@emnapi/*` 等条目，导致 CI 的 `npm ci` 报 not in sync。
- 站内链接不要手拼，教程走 `slugOf()`，作品和器材用数据里的 `slug` 字段。
- 教程 →「去实验室试试」入口的映射集中在 `src/data/labEntries.ts`，不要散落到
  Markdown 正文里；新增时注意快门写法（`125` 即 1/125s，整秒带引号如 `2"`），
  并确认预设在该场景下曝光落在 ±1EV 内。
- 相机模型的视角预设、部件与教程映射分别放在 `src/components/lab/cameraViews.ts`、
  `cameraParts.ts`；新增部件时同时在 `cameraGeometry.ts` 的 `interactive` 表里登记
  同名 id，否则点不中。
- 相机模型新增部件的完整清单：`cameraParts.ts` 里补 `CameraPart`（含 `bestView`，取值
  必须是 `cameraViews.ts` 里存在的视角 id）、把 id 加进 `PART_GROUPS` 对应分组，
  再在 `cameraGeometry.ts` 里用 `addPart(id, [mesh, ...])` 登记。`addPart` 会把这些
  mesh 收进独立 group，射线命中任一子 mesh 都算选中整个部件，不要再直接
  `interactive.set(id, mesh)`。
- 相机模型的视角切换是限时补间：`runtime.flight` 存着起止位置与时长，`t >= 1` 时必须
  置回 `null` 把控制权交还 OrbitControls。不要改回每帧朝目标 lerp，那样用户拖动会被
  往回拽。

## 版本历史

- v0.1.0 项目初始化
- v1.0.0 首页 / 教程 / 作品 / 装备 / 关于全部页面打通，接入深浅色切换、社交分享、访问统计

（实验室模块的曝光模拟器与相机模型已在 v1.0.0 之后落地，尚未打新的版本号。）
